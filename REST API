import json
from flask import Flask, request, jsonify

app = Flask(__name__)

@app.route('/', methods=['GET','POST'])
def index():
result = "result"
return jsonify(record)
if__name__=='__main__':
app.run(debug=True)
